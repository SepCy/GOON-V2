import React from 'react';

type Props = {
  category: any;
};

const CategoryCard: React.FC<Props> = ({ category }) => {
  return <div className="lg:flex lg:flex-col">{}</div>;
};

export default CategoryCard;
